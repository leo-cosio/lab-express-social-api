const { Router } = require("express");

const router = Router();

// TODO Iteracion 3: Montar el router de usuarios
//   const usersRouter = require("./users.controller");
//   router.use(usersRouter);

// TODO Iteracion 4: Montar el router de posts
//   const postsRouter = require("./posts.controller");
//   router.use(postsRouter);

// TODO Iteracion 5 [Bonus]: Montar el router de comments
//   const commentsRouter = require("./comments.controller");
//   router.use(commentsRouter);

module.exports = router;
